name: Install2
description: DummyInstall2
schemaVersion: 1.0
phases:
    - name: build
      steps:
        - name: print
          action: ExecuteBash
          inputs:
            commands:
                - echo "Second"
                - sudo echo Second >> ~/test.txt
                - sudo cat ~/test.txt