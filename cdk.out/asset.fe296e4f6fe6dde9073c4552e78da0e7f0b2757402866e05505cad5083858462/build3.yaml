name: InstallPython3Yum
description: this document installs python3 on ubuntu
schemaVersion: 1.0

phases:
    - name: build
      steps:
        - name: UpdatePackage
          action: ExecuteBash
          inputs:
            commands:
                - echo "Third"
                - sudo echo Third >> ~/test.txt
                - sudo cat ~/test.txt