name: InstallPython3Yum
description: this document installs python3 on ubuntu
schemaVersion: 1.0

phases:
    - name: build
      steps:
        - name: InstallPython3
          action: ExecuteBash
          inputs:
            commands:
                - echo "Installing Pip3"
                - sudo sudo yum install -y python3-pip