name: InstallPython3Yum
description: this document installs python3 on ubuntu
schemaVersion: 1.0
parameters:
  - testparam:
      type: string
      default: "test"
      description: "File path for payload download."
phases:
    - name: build
      steps:
        - name: UpdatePackage
          action: ExecuteBash
          inputs:
            commands:
                - echo "First"
                - sudo echo Build6 > ~/test.txt
                - sudo cat ~/test.txt